extends layout

include _includes/sharedHTMLfunctions

block content
  .row.banner
    .col-12
      h1= pageHeader.title
        small &nbsp;#{pageHeader.strapline}
  .row
    .col-12.col-md-8
      .error= message
      each shop in shops
        .card
          .card-block
            h4
              a(href=``)= shop.name
              small &nbsp; Expires: #{shop.experation}
              span.badge.badge-pill.badge-default.float-right= shop.distance
            p.address= shop.description
              span.badge.badge-pill.badge-default.float-right= `€${shop.price}`


    .col-12.col-md-4
      p.lead= sidebar